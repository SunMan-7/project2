table:
  name: subprojects
  schema: public
array_relationships:
  - name: deployments
    using:
      foreign_key_constraint_on:
        column: subproject_id
        table:
          name: deployments
          schema: public
